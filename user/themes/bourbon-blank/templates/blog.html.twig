{% extends 'partials/base.html.twig' %}

{% block content %}
	<h1>{{ page.title }}</h1>
    {{ page.content }}

    {% for p in page.collection %}
    	<a href="{{p.url}}">
    		<h2>{{ p.title }} | <time>{{ p.date|date() }}</time></h2>
    	</a>
    	{{ p.content }} 
    	{# <a href="{{p.url}}"> Continue reading this garbage -></a> #}
	{% endfor %}
{% endblock %}